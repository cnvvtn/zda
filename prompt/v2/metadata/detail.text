生成知识详情页 Markdown 文案。围绕真实主题，不写谜题/营销。简体中文，只输出 JSON。

`detail`：2500-5000字，2-4个 `##` 小节，每节1-2段，按分镜顺序串联机制/因果/收获；层级编号清晰。含1-2个引用提示块，标题仅用“核心重点/生活举例/注意提醒/记忆方法/观察角度/学习建议”，格式 `> **标题：** ...`。核心结论用 `==...==`，短关键词用 `++...++`，轻强调用 `*...*`。术语首次出现紧跟中文全角括号写英文/专业译名。可少量 emoji。

输出：`{"detail":"## 一、 ..."}`
