Generate complete `TimelineData` code for the subject.

Rules: canvas 1920x1080 center (960,540), absolute coords; choose One-Take/Hard Cut/Match Cut; first scheduled item is a custom theme background; concrete textured 2D SVG + Emoji states; physical staggered motion.

API: `setCamera` x[-8,8] y[-2,6] scale[0.5,2]; `createElementWithDualLayer` math coords only; `removeElement`; `clearScene`; `createPath(getPathD(points),...)`; `scheduleAnim`.

Fatal SVG: no `<svg>`/viewBox/width/height; root `<g id="[asset]_root">`; defs ids `[asset]_...`; centroid/pivots `(0,0)`; geometry 100x; stroke >=5; text >=35 centered; no `document.xxx`.

[Subject & Theme]: 自动扶梯扶手带为何比梯级快
[Expected Number of Scenes]: 8

Output code only:
const palette = { bg: "[HEX]", primary: "[HEX]", highlight: "[HEX]" }
const SVG_ASSETS = { ... }
var TimelineData = [{ texts: { vivid: "Chinese vivid lines", ext: "Chinese professional lines", en: "English lines" }, onEnter: () => { scheduleAnim([]); } }];
const dynamicCSS = `<style id="dynamic-css">......</style>`;
