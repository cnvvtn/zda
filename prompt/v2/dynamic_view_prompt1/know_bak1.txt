Generate complete `TimelineData` code for Chinese knowledge animation.

Rules: canvas 1920x1080 center (960,540), absolute coords; choose One-Take/Hard Cut/Match Cut, cleanup on Hard Cut; SVG macro scene + SVG/Emoji micro states; motion has physical feedback, stagger, and narration sync; each shot has background/main/dynamic-data layers; main nodes breathe/hover; paths trigger packets and target feedback; visuals are concrete, textured, domain-specific, not flat.

Fatal SVG: no `<svg>`/viewBox/width/height; root `<g id="[asset]_root">`; defs ids `[asset]_...`; centroid `(0,0)`; pivots `(0,0)`; geometry 100x; stroke >=5; text >=35 centered; asset layers `[asset]_ambient/body/core`.

API: `setCamera` x[-8,8] y[-2,6] scale[0.5,2]; `createElementWithDualLayer` math coords only; `removeElement`; `clearScene`; `createPath(getPathD(points),...)`; `scheduleAnim` stagger 200-500ms.

Output complete code only:
const palette = { bg: "[HEX]", primary: "[HEX]", highlight: "[HEX]" }
const SVG_ASSETS = { ... }
var TimelineData = [{ texts: { vivid: "Chinese vivid lines", ext: "Chinese professional lines", en: "English lines" }, onEnter: () => { /* Script Anchor + Transition Strategy */ scheduleAnim([]); } }];
const dynamicCSS = `<style id="dynamic-css">......</style>`;
