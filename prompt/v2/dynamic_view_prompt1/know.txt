Generate complete `TimelineData` code for a Chinese science animation.

Scene: canvas {{CANVAS_RESOLUTION}}; use math coordinates only. Pick One-Take / Hard Cut / Match Cut per scene; Hard Cut calls `clearScene`. First scheduled item mounts a self-created theme background. All motion needs easing/curves/feedback/stagger. Use concrete domain-specific 2D SVG with gradients/shadows/particles/micro-motion; reject flat generic shapes. {{VISUAL_STYLE_OVERRIDE}}

API: `setCamera(x,y,scale,duration)` x {{MATH_X_RANGE}}, y {{MATH_Y_RANGE}}, scale [0.5,2]; `createElementWithDualLayer(x,y,anim,svg,persist,id)` math coords only; `removeElement`; `clearScene`; `createPath(getPathD(points),stroke,width,class,persist)`; `scheduleAnim`; `mapCoords` uses {{MATH_X_RANGE}}/{{MATH_Y_RANGE}}, {{ORIGIN_DESCRIPTION}} X right, Y up.

Fatal SVG: no `<svg>`, `viewBox`, `width`, `height`; root `<g id="[asset]_root">`; defs ids `[asset]_...`; centroid `(0,0)`; rotating pivots `(0,0)` with translate; geometry 100x; stroke >=5; text >=35 and centered; no `document.xxx`.

[Subject & Theme]: XXX
[Expected Number of Scenes]: N

Output complete code only, no export/TODO:
const palette = { bg: "[HEX]", primary: "[HEX]", highlight: "[HEX]" }
const SVG_ASSETS = { ... }
var TimelineData = [{
  texts: { vivid: "{{LANGUAGE1}} vivid narration", ext: "{{LANGUAGE1}} professional narration", en: "{{LANGUAGE2}} lines" },
  onEnter: () => {
    // Transition strategy method: One-Take / Hard Cut / Match Cut. Reason: ...
    scheduleAnim([]);
  }
}];
const dynamicCSS = `<style id="dynamic-css">......</style>`;
