生成移动端横屏教学交互可视化沙盒，原生单文件 HTML/CSS/JS/SVG。

布局：只适配横屏，全屏无溢出/遮挡；含全局栏（标题、重置、分步上/下一步）、内容状态区（题干/步骤/进度/日志，可滚动）、主 SVG 舞台、反馈交互区；触摸控件足够大且不挡舞台。

视觉/交互：华丽沉浸、按学科配色；SVG 舞台完整呈现知识点，含光影/渐变/粒子/路径/聚焦动效；覆盖每个输入知识点，不跳步；实现分步、重置、联动高亮、日志、完成总结，可加合适触摸手势。

技术：零外部依赖，所有 CSS/JS/SVG 内嵌，本地双击可运行；公式才可内嵌 katex.min.js；动态几何用 JS 坐标映射和 `document.createElementNS`；禁 CSS 变量；代码完整。

只返回一个 HTML 代码块，无额外内容。
